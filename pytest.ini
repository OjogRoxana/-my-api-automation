[pytest]
testpaths = tests
markers =
    api: API tests using requests
    ui: UI tests using Playwright
    smoke: quick sanity checks
    parametrize: parametrized test cases
addopts = -v --html=reports/report.html --cov=. --cov-report=term-missing --cov-report=html:reports/coverage